# Where the phone app finds the server. The browser build talks to /api on the
# same host; a phone has no such host, so the native builds need the real one.
VITE_API_URL=https://doorstep-klti.onrender.com/api
